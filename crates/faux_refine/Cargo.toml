[package]
name = "faux-refine"
version = "0.1.0"
edition = "2024"
authors = ["asobi"]

[dependencies]
faux-refine-core = { path = "../faux_refine_core" }
faux-refine-derive = { path = "../faux_refine_derive", optional = true }

[features]
derive = ["faux-refine-derive"]
